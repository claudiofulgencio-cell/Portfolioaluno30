
  # Revise portfolio layout

  This is a code bundle for Revise portfolio layout. The original project is available at https://www.figma.com/design/mem60AOq0NsnMp3ccStnIf/Revise-portfolio-layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  